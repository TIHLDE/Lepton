language: python # => 1
    python: # => 2
      - "3.6"
    services: # => 3
      - mysql
    env: # => 4
      -DJANGO=2.1.7 DB=mysql
    install: # => 5
      - pip install -r
    before_script: # => 6
      - mysql -e 'create database test;' -u root
    script: # => 7
      - python manage.py test
      
branches:
    only:
    - master
    - dev
